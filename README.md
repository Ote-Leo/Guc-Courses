# GUC Cources

This repository is intended for all GUC Semester 9 Courses, that includes:

1. CSEN 1022 - Machine Learning
2. CSEN 1095 - Data Engineering
3. DMET 901 - Computer Vision

# Set Up

Initializing a python virtual environment. All dependencies are in the `REQUIRENMENTS` file. Go to the repository directory, and run the following commands:

```sh
    >>> python -m venv .env
    >>> .env/Scripts/activate
    >>> python -m pip install -r REQUIRENMENTS
```
